import style from './HomePage.module.css';

const Homepage = () => {
  return (
    <h1 className={style.title}>
      Welcome, register or login to see your contacts list
    </h1>
  );
};

export default Homepage;
